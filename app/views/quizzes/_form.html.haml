%p
  = f.label :category_id
  = f.collection_select :category_id, Category.for_select, :id, :name
%p
  = f.label :title
  = f.text_field :title, :class => 'big'
  = f.error_message_on :title, :prepend_text => 'Title '
%p
  = f.label :description
  = f.text_area :description